This directory contains files shared between the Cloud Files and Cloud Servers unit tests to make them easier to merge later.

Anything that I reused from Cloud Files is in here, just diff the versions to see whether I had to make any changes for Cloud Servers but the changes were small and they should be very easy to merge.